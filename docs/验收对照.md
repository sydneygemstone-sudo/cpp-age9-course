# 一期验收对照表

> 依据 `docs/方案定稿.md` §12.2 逐条整理，一条不增、一条不减。
> 状态说明：**待集成核验** = 各模块已按契约实现/交付，但需在集成阶段以实际运行核验；QA 结论由集成/QA 阶段填写，本表交付时留空。
> 核验时同时对照 CONTRACT §14 八条硬性红线（不伪造真实编译、儿童端不显示 E/S/A 与分数、不采集真实身份信息、无 AI 时全流程可用、主按钮 ≤2、报告须教师确认、试听诊断期不开放儿童提示、内容与引擎分离）。

> **集成核验记录（2026-08-05）**：五套 Node 测试全绿（test-ir 114 / test-engine 71 / test-compiler 91 / test-evidence 64 / test-content-schema 1512 断言）；脚本顺序与 CONTRACT §2 逐行一致；全部 CppLab.X.y 跨模块引用经 Node 加载核对存在；file:// 兼容扫描（无 import/export/eval/本地 fetch/CDN）通过；Chrome 实测走通试听 1-4/10、lesson1 开场、lesson2-07 换门全流程与教师端 PIN 登录。仍标「待集成核验」的条目需真人课堂/QA 场景核验（模拟学生、教师全程盲跑、90 秒首胜计时等）。
>
> **对抗性 QA 修复记录（2026-08-05）**：三路对抗 QA 的 9 条 P1 全部修复、14 条 P2 修 12 条（skip 2 条：加速线 multiState 判据收紧、CE 备用编译器 id——理由见提交说明），修复后五套测试仍全绿。要点：
> ① bughunt 双活动 expectedStdout 改存修好版输出，产品比对基准改为「实际送编译程序」的 IR 模拟 stdout，本机 clang++ -std=c++17 实证 6 档（trial-05 三档「门开了！」/ lesson2-05 三档「OPEN」）真实输出与模拟、expectedStdout 逐字一致；schema 测试补 bughunt 修好版特例，带 bug 输出无法再蒙混绿灯。
> ② lesson1-02 排序卡驱动程序执行：交换顺序→代码台跟随→重跑可见中间状态差异（浏览器实测 3→2→6 vs 3→7→6），完成后仍可交换重跑（不再计证据）。
> ③ 自动评分封顶 outcome=2，3 级只能教师基于解释/迁移证据手动升级（防加速线 canExplain 被一次点选满足）。
> ④ lesson1.finalEnergy 权威来源单点化：作品卡保存优先、act5 达标次之，引擎演示重跑不再覆写（test-engine 断言同步更新）。
> ⑤ 作品卡补齐方案 7.7 的「最终结果 + 一句自己的解释」。⑥ 教师中途调档 ≤2 秒同步儿童端（未开始活动立即换变体，浏览器实测 S→E 生效），教师端文案与真实行为一致。⑦ trial-02/trial-08-E 排对后有逐步命令动画旁白，intro 文案与实际交互一致，无程序活动不再出现「跑一跑」死指引。⑧ 教师端补「教师笔记（课中随手记）」卡与「真实编译通道」状态卡（降级原因透出）。⑨ Wandbox 保守闸门：非学生程序特征的失败（126/127、无输出无报错的非零退出）一律降级 offline，不再误判 runtime_error。

## A. 教育有效性

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| A1 | 试听核心维度均有直接证据，或明确标记「N：未观察」 | 已核验 | test-evidence（summarize 未观察维度默认 N）+ 集成冒烟：试听 10 活动逐一完成均落证据 |
| A2 | 每个核心概念至少出现一次教学后迁移 | 待集成核验 | |
| A3 | 不依靠数学总分决定路径 | 已核验 | test-evidence 64 断言：pathSuggestion 按方案 5.3 含两条限制，不用数学总分 |
| A4 | 第 1、2 课各至少有三次「预测—运行—解释」循环 | 待集成核验 | |
| A5 | 孩子无需在前 20 分钟书写完整程序 | 待集成核验 | |
| A6 | 每课均产生可向家长展示的作品 | 待集成核验 | |
| A7 | 第 2 课确实读取第 1 课状态，而不是视觉上假装连续 | 已核验 | 集成冒烟：lesson1.finalEnergy 真实注入 lesson2-01 program 与 prediction.correct，承接开场文案生成；2026-08-05 QA 修复后 finalEnergy 权威来源单点化（作品卡优先/act5 达标次之，演示重跑不覆写），承接值与孩子真实经历一致 |

## B. 儿童体验

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| B1 | 进入活动后 90 秒内有第一次可见成功 | 待集成核验 | |
| B2 | 单个界面不同时高亮三个以上主要操作 | 已核验 | app.js refreshButtons 全局状态机（.btn-primary≤2）代码走查 + 浏览器实测各阶段高亮 |
| B3 | 支持重置，且不会因失败丢失课程进度 | 已核验 | test-engine：reset 不丢课程进度/证据/提示记录；浏览器实测重置后活动可重跑 |
| B4 | 错误提示先解释动作，再显示工程术语 | 已核验 | test-compiler 91 断言 + 真实 CE 实测：缺分号返回「编译器在第7行附近没有找到分号」中文先行 |
| B5 | 不使用只有颜色才能区分的反馈 | 已核验 | 可视化真✓/假✗图标文字并用（原有）+ 选项 chip 对/错补 ::after ✓/✗ 图标（2026-08-05 QA 修复，浏览器 computed style 实测生效） |
| B6 | 动画和声音均可关闭 | 已核验 | 浏览器实测：右上角「动画：开/关」「声音：开/关」两开关存在且可切换，设置持久化 |

## C. 教师可运行性

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| C1 | 每项活动均包含：正确心智模型、正确结果、演示步骤、三个追问、常见误区、降阶提示、加速扩展 | 已核验 | test-content-schema 1512 断言：26 活动 teacherCards 六件套/hintLadder 5 级 H5 teacherOnly 全部通过 |
| C2 | 教师不查看源代码仓库，也能从头完成课程和报告 | 待集成核验 | |

## D. 技术真实性

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| D1 | 标准样例在固定 C++ 环境（C++17）真实编译 | 已核验 | 2026-08-05 真实调用 Compiler Explorer（x86-64 gcc 14.3）：lesson1-04 程序编译运行 stdout="6" |
| D2 | 故意制造的语法错误能返回真实错误 | 已核验 | 同日实测：删除分号后返回 compile_error 与真实 g++ stderr 映射的中文友好提示 |
| D3 | 概念模拟与真实编译在标准任务中结果一致 | 已核验 | 实测真实编译 stdout 与 IR.execute 逐字节一致（含 UTF-8 中文输出）；2026-08-05 QA 修复：bughunt 修好版比对错位已消除，产品比对基准=实际送编译程序的 IR stdout，6 档修好版经本机 clang++ -std=c++17 实证一致 |
| D4 | 无法可视化的自由代码不会被假动画覆盖（明确显示控制台模式） | 已核验 | app.js freeEdit 渲染代码走查：「控制台模式」徽章 + 明示不驱动左侧画面，无假动画 |
| D5 | 编译器不可用时明确标注，不显示伪「验证成功」 | 已核验 | test-compiler offline 语义 + app.js renderCompileResult：real:false 一律「⚠️ 概念演示」，无假验证成功 |
| D6 | 浏览器端不包含编译服务密钥 | 已核验 | 全仓 grep：无任何 key/token/secret，两端点均为公共匿名 API |
| D7 | 编译请求具有超时、限流和输出限制 | 已核验 | compiler.js 配置核对 + test-compiler：15s 超时（AbortController）、全局串行 ≥1s 节流、stdout 10KB 截断 |

## E. 报告可信度

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| E1 | 自动报告区分「已观察、初步判断、待验证」 | 已核验 | test-evidence：每条 item 的 tag 必须是 已观察/初步判断/待验证 之一 |
| E2 | 每项关键判断能回链到具体任务证据 | 已核验 | test-evidence：已观察条目必须带 evidenceRefs 回链断言通过 |
| E3 | 教师确认前不能生成最终家长版 | 已核验 | test-evidence：无 confirmedAt 调 printableHTML 抛错；打印页只吃 confirmed |
| E4 | 支持修改错误记录（保留原值） | 已核验 | test-evidence：overridden.original 保留原值，反复覆盖不丢最初原值 |
| E5 | 不输出智力、人格或长期能力结论 | 已核验 | test-evidence：含「智力」等禁用表述的报告 confirmReport 拒绝并抛错 |

## F. QA 场景

### F-1 三名模拟学生完整跑通

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| F1 | 白纸学生：需要图块和多次提示，全程可完成 | 待集成核验 | |
| F2 | 图形化编程学生：能理解规则但不熟悉文本语法，全程可完成 | 待集成核验 | |
| F3 | 已有文本代码学生：能快速进入自由编辑和边界调试，全程可完成 | 待集成核验 | |

### F-2 关键场景覆盖

| 编号 | 验收标准 | 状态 | QA 结论 |
|---|---|---|---|
| F4 | 中途上调路径 | 已核验 | test-engine：连续 2 活动高分低提示触发 scaffold-suggest up（单活动不触发） |
| F5 | 中途下调路径 | 已核验 | test-engine：连续 2 活动 S2+ 提示后低分触发 down 建议，只建议不自动切换 |
| F6 | 教师人工覆盖（记录覆盖原因） | 已核验 | test-engine setScaffold 留痕断言 + teacher.js 代码核对：教师覆盖必填原因，记录 {at,from,to,by:"teacher",reason}；2026-08-05 QA 修复：调档 ≤2 秒自动同步进行中的儿童会话（未开始活动立即换变体，浏览器实测 S→E 生效），课中调档真实可运行 |
| F7 | 网络断开（降级为概念演示并明确标示） | 已核验 | 集成冒烟：远程不可达/Mock 无 program 时降级 offline，UI 显示「⚠️ 概念演示——真实编译暂不可用」 |
| F8 | 编译超时（15 秒终止并明确提示） | 已核验 | compiler.js timeoutMs=15000 + test-compiler 超时分支返回 timeout 状态与儿童化提示 |
| F9 | 刷新后恢复（localStorage 会话恢复） | 已核验 | 浏览器实测：刷新后会话恢复，lesson2 直接续位到第一个未完成活动，进度/兴趣/aiHabit 均保留 |
| F10 | 删除会话 | 待集成核验 | |
| F11 | 生成和编辑报告（草稿→编辑→确认→打印链路） | 已核验 | test-evidence 草稿→确认→打印链路断言 + teacher.js 报告区接线核对（编辑/确认/打印按钮齐全） |

## 二轮迭代记录（codex 双路 QA + 修复，2026-08-05）

二轮由 codex 双路对抗 QA（UI 路 + 教案/内容路）发起，两路问题单的 P0/P1 全清、P2 按裁定处理，另清掉一轮遗留的建造项 10 条；修复后五套 Node 测试全绿（test-ir 114 / test-engine 71 / test-compiler 91 / test-evidence 64 / test-content-schema **1513** 断言），全部 js 通过 `node --check`，教案引用的按钮名/屏幕文案经临时 Node 脚本与 app.js/content 逐串比对通过。红线复查：主按钮 ≤2、真实编译诚实标记、儿童端零 E/S/A 泄露、file:// 零依赖均未被本轮改动破坏。

### 跨文件新约定（已写入 CONTRACT §13-2 C，内容侧与 UI 侧共同遵守）

1. **freeEdit 完成判定**：`interaction.expectedStdout`（内容侧提供）；有 prediction 必须已提交预测 + 真实编译 `real:true && ok` + `stdout.trim()===expectedStdout` 三者齐备才完成；不匹配只展示真实输出并提示再试（trial-08-A"不改代码直接验证也过关"的漏洞堵死）。
2. **freeEdit 离线通道**：`real:false` 时显示「📴 现在没网，真实编译用不了。请老师看过你的代码后确认」+「👩‍🏫 老师确认完成」；点击完成、证据 childAction 记「离线-教师人工确认」、outcome 记 N，绝不打真实验证标记（红线 §14.1 不变）。
3. **两段式 predict**：同时带热身 prediction 与 interaction 主提问时，预测+运行后弹「⭐ 主问题」，完成判定看主提问（trial-03 三档）；同题双写（lesson2-06 E/A）判单段式；bughunt/teach-transfer/slots 带 prediction 时运行结束走各自类型判定，预测不短路完成。

### UI 路修复要点（codex-UI P1/P2）

- lesson1-07「用真实C++验证我的故事」在拼出有效程序前置灰 + 原因说明（title + 按钮下小字），选好出发能量即时解禁（冒烟覆盖）。
- 767px 布局核验不破；CONTRACT §13-2 B.3 改写为真实行为（<768 前两栏堆叠单列、状态透镜维持抽屉，Dean 裁定）；A.1「真实C++验证」渲染条件改写为与 activityCanVerify 一致（含 free 模式例外）。
- pointer:coarse 触控目标补足 44px（brand/可点代码行/槽位 chip/主题卡/新增可点标识符等）。
- 儿童阅读文字统一 ≥17px（场景目标/时间线/提示/错误卡/toast/空态/可视化文案等 16/15→17，装饰性小标签保留）。
- 「📊 状态」FAB 用 ResizeObserver 跟随按钮条实际高度（--btnbar-h），不再被遮挡；预测面板加 scroll-margin 防极矮视口遮挡提交键。
- 全屏/添加到主屏幕场景补 env(safe-area-inset-*) 回退间距；teacher.html PIN 卡宽度 min(340px, calc(100vw−24px)) 防窄屏溢出。

### 教案/内容路修复要点（codex-教案 P0/P1/P2 + 建造遗留）

- P0-1/P0-2：freeEdit 完成判定与离线通道按跨文件约定 1/2 实现（冒烟覆盖）；trial-08-A 增 `expectedStdout:'启动成功'`（本机 clang++ -std=c++17 实证，任何合法一行修法同果）。
- P0-3：底册整数除法归因改正（整数除法本身向零截断，非"装进 int 盒子"；补 7.0/2=3.5 对照与"存入 int 再转换"的分层）。
- P0-4：底册花括号说法改正（单语句省略花括号合法；课程保留花括号是防控制流 bug 的约定；"不配对"与"成对省略"分开讲）。
- P0-5：函数机器术语纠偏（trial.js teacherCards + 试听教案④）：power=power+3 统一称「输入—处理—输出规则机器的代码预览」，明确无参数无 return、真函数第 5 课才学、std::cout 是屏幕输出非返回值。
- P1-6：底册 && 补短路（左假右不查）+ Q&A + 速查表。
- P1-7（=约定 3）：lesson2-06 S 档改 activityTypeOverride:'slots'（单槽「能量」number，goal stdout 'CHARGE'），流程改为"用 5 真实编译看 OPEN → 亲手改 4 看 CHARGE"；compilerCheck.expectedStdout 保持未改动程序的 'OPEN'（engine.getVariant 透出 override 已浏览器实测）。
- P1-8：三份教案界面总览按动态 UI 重写（按钮/代码台/右栏按活动动态显示），全部「按钮变灰」表述清除；试听 1–9 儿童端无「求提示」按钮、提示由教师端读取，相关小节逐处订正。
- P1-9：试听教案活动 4 组件名与检查点题目改为屏幕实际字符串（📦 变量卡、🔍 侦探提问、⭐ 再来一个小挑战及三档真实题目），口头概括处标注「老师概括」。
- P1-10：底册术语速查表补齐（函数机器/输入/处理/输出含第 5 课预告、</<=、条件、bug、边界测试、&& 短路）。
- P2-11：底册「只在过一个」病句改「任何时候只装一个」。
- 建造遗留 1–10：两段式主提问、onRunDone 类型分流（trial-05 证据完整保留、trial-08-S/lesson2-05 判定恢复、teach-transfer 运行改 toast 引导）、trial-08-S 槽位预填改非解「加 3 放 1」（stdout 8）、trial-01 去掉不存在的"点选搭档"交互、teach-transfer 舞台指令不显示改驱动 demoFeed 动画、build 预演区新增只读「👀 变成的代码」、choice 槽 applyValue 显示 label、C++ 标识符中文释义触屏点按可用、Visualizer noProgram 选项（无程序活动场景不渲染变量盒/输出屏）、lesson1-06 prediction/interaction 冗余清理（无行为变化，A 档括号提示并入 prediction）。

### 文档同步（本记录所在轮）

- 四份教案 + 教师手册按上述行为变化全量同步：试听总览/活动 1/3/4/5/6/8/10、第 1 课界面速览与活动 1/2/3/4/7、第 2 课总览与活动 2/5/6（S 档新节拍表）、教师手册 §2.1 动态界面与 freeEdit 完成判定、FAQ Q1 补自由编辑离线"教师确认完成"预案。
- 教案中引用的屏幕文案与 app.js/content 逐串核对（临时 Node 脚本，含 trial-03 主问题、trial-04 三档检查点与小挑战、trial-08-S 槽位与预填 stdout=8、trial-08-A expectedStdout、lesson2-06 S 档槽位/goal/改 4 后 IR stdout=CHARGE、离线通道与 build 预演区等 30+ 字符串）：全部一致。

### 遗留（不阻塞）

- 一轮 skip 的 2 条 P2 维持原裁定（加速线 multiState 判据收紧、CE 备用编译器 id）。
- 「待集成核验」条目（真人课堂/盲跑/90 秒首胜计时等）仍待真实课堂场景核验，本轮不变更其状态。

## 主题一致性修复记录（2026-08-05）

**问题**：Dean 实测选「宠物乐园」主题后，任务卡仍显示「捡电池」等机器人世界词汇——主题皮肤只换了立绘，没换故事词汇。codex 对三主题文案做全量盘点（`docs/theme-qa-inventory.json`，共 118 条：违和-必换 68 / 边缘-建议换 22 / 中性-保留 13 / 教学锚点-禁动 15），问题集中在 trial.js 电池/飞船救援链、lesson1.js 机器人角色与作品事件、lesson2.js 舱门叙事及四份教案的对应口播。

**总架构裁定**：内容文件 `js/content/*.js` 保持「机器人世界」为正本、零改动；主题化在**渲染层**实现——theme.js 词典按「正本词 → 宠物乐园 / 探险王国」做最长优先匹配替换，三主题 id 沿用 `session.theme` 现值（robot / pet / adventure）。核心词典（智能舱门→聪明园门/机关石门、电池与能量豆→能量罐头/能量水晶、机器人→小狗搭档/探险搭档、飞船→乐园救援车/探险飞艇 等 16 组）同一道具全课程同词。

**保护面（优先于词典，不过替换）**：「机器人世界/宠物乐园/探险王国」identity 词条；皮肤名三主题统一为「蓝色圆滚滚/橙色方块侠/绿色电力狗」；代码台/聚焦代码/完整程序、编译器输出与友好错误、`OPEN`/`CHARGE` 等字面量、C++ 标识符释义表、onboarding 主题选择卡（单独手改）、证据 childAction、家长报告正文；「能量」一词与能量条格数体系全课程保留。红线复查：主按钮 ≤2、真实编译诚实标记、儿童端零 E/S/A、file:// 零依赖、Node 兼容均不受影响。

**教案侧同步（本记录所在轮）**：四份教案（试听/第 1 课/第 2 课/白话底册）开头各插入「主题词汇对照表」小节（核心词典 16 组 + 教案长尾词 + 跨主题不变清单），教师照表换词朗读；底册 std::cout「机器人的喇叭」四处加主题括注；试听活动 2 节拍表加「主题词见对照表」总标注一次；第 1 课活动 7/8 开场讲稿后附两主题变体口播；教师手册 §2.1 补「主题皮肤只换故事词汇、教学内容/代码/评分一致」段落。盘点清单中 surface=教案文档 的 23 条由上述对照表说明+就地加注覆盖。
